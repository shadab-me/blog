<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= article.title %> </title>
  <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.14.0/css/all.css">
  <link rel="stylesheet" href="/stylesheet/article.css">
</head>
<body>
  <%- include("_header.ejs") %> 
  <main>
    <div class="container">
      <div class="breadcrumb align-left">
       </div>
      <h2 class="heading"><%= article.title %></h2>
      <p class="article-desc"><%= `${article.content}` %> </p>
      <div class="social align-left">
        <a href="https://www.facebook.com/sharer/sharer.php?u=<%= "http://localhost:3000/" + article.id %>" target="_blank"><i class="fab fa-facebook"></i></a>
        <a href="https://www.facebook.com/sharer/sharer.php?u=<%= "http://localhost:3000/" + article.id %>" target="_blank"><i class="fab fa-twitter"></i></a>
        <a href="https://www.facebook.com/sharer/sharer.php?u=<%= "http://localhost:3000/" + article.id %>" target="_blank"><i class="fab fa-linkedin-in"></i></a>
      </div>
    </div>
    <div class="container">
      <h3 id="dis">Discussion</h3>
      <div class="comment-form">
        <form action="/articles/<%= article.id %>/comment" method="post">
            <textarea name="comment" cols="30" rows="5" placeholder="Add to the discussion"></textarea>
           <input type="submit" value="Submit">
        </form>

        <div class="comments">
          <% article.comments.forEach(comment => { %>
            <div class="comment">
              <h4><%= comment.name %></h4>
              <h6><%= comment.comment %></h6>
               </div>
            </div>
          <% }) %>

        </div>
      </div>
    </div>
  </main>
 </body>
</html>